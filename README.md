# ACA-ResUNet

使用时需要准备好数据集：1、在主目录创建data文件夹，在data文件夹下创建images和labels文件夹，分别放jpeg格式和json格式的数据集

运行前先配置好环境，安装依赖：requirements.txt

## 模块说明
- DMS = ASF (Adaptive Scale Fusion)
- GFF = HFC (Hierarchical Feature Compensation) 
- ASPP = MPCA (Multi-Scale Pyramid Context Aggregation)

